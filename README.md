# NexusPlatform Portfolio — Index

> Senior .NET &amp; Data Engineer portfolio by **Greg Zapantis**. This repository is the
> **start here** for recruiters, CTOs, and prospective clients. It maps every project
> in the portfolio to the skills it demonstrates.

<p align="center">
  <a href="https://github.com/grezap">👤 GitHub Profile</a> &nbsp;·&nbsp;
  <!-- <a href="https://gregzapantis.dev">🌐 Website</a> &nbsp;·&nbsp; -->
  <a href="https://www.linkedin.com/in/grigoris-zapantis-1a0638b/">💼 LinkedIn</a> &nbsp;·&nbsp;
  <a href="./PORTFOLIO.md">📊 Skills matrix</a> &nbsp;·&nbsp;
  <a href="https://github.com/grezap/nexus-platform-plan">🗺️ Master plan (nexus-platform-plan)</a> &nbsp;·&nbsp;
  <a href="https://github.com/grezap/nexus-platform-plan/blob/main/docs/glossary.md">📖 Tool stack glossary</a>
</p>

> **New to the stack?** The portfolio uses a lot of named tools — Vault, Consul, Nomad, Kafka, Iceberg, ClickHouse, StarRocks, Prefect, Marquez, … — that not every reader has encountered. Open the [tool stack glossary](https://github.com/grezap/nexus-platform-plan/blob/main/docs/glossary.md) for plain-English definitions of each, grouped by where in the stack they sit.

---

> **📘 Canonical blueprint:** The complete end-to-end plan — 14 app projects, 11 built
> infrastructure repos, 30 enhancements, 12 build phases, a 143-VM + 9-VIP lab topology
> (built/cold-rebuild-proven through Phase 0.P plus the 0.N.1 sharded-Mongo wire mTLS + 0.O.1 Vitess
> engine-native backup hardening + the platform CA rollover + **Phase 0.Q.1 Marquez/OpenLineage tier
> SEALED 2026-07-21**), 22 demo playbooks, and all per-project schema designs — lives in
> **[`nexus-platform-plan`](https://github.com/grezap/nexus-platform-plan)**
> (v0.1.0 Plan published). Start there for the architectural source of truth.

## What is NexusPlatform?

NexusPlatform is a collection of **14 production-grade .NET application projects**,
**11 built infrastructure repositories**, and a **4-app native Windows suite**, engineered as
a single coherent portfolio. Every project is built to senior-engineering standards:
correct architectural patterns, HA-capable infrastructure, full observability,
automated tests, and documented runbooks.

It targets four readers:

- **Enterprise Data Architects** — looking for modern data-platform expertise.
- **.NET Technical Leads / CTOs** — looking for architectural judgement and full DevOps ownership.
- **AI-Forward Businesses** — looking for ML/LLM systems that ship.
- **Upwork / freelance clients** — looking for polished, working demonstrations of capability.

## How to navigate this portfolio

1. Start with the [**skills matrix**](./PORTFOLIO.md) if you're filtering by technology.
2. Browse the [**project grid**](#project-grid) below if you want the one-paragraph pitch.
3. Click into any project repository for full architecture, demo, and case study.

## Project grid

> Legend: 🟢 shipped · 🟡 in progress · ⚪ planned

### Infrastructure

| Status | Project | What it is |
|:---:|---|---|
| 🟢 | [`nexus-cli`](https://github.com/grezap/nexus-cli) | .NET 10 Native AOT CLI — single binary, controls the entire VM/Swarm/Nomad/Kafka fleet. **Now at `v0.8.12` (2026-07-10) — 🏁 100% COMPLETE**: all 13 GAPs closed + infra-hardening 4/4 done + GENUINE-NA rulings reviewed+confirmed (330 tests, AOT 28.34 MB). Post-full-fleet, **v0.8.8–v0.8.12** delivered the infra-hardening block — the **0.N.1 sharded-Mongo wire mTLS** (MongoShardedAdapter cert-rotate now LIVE — online `rotateCertificates` on the per-host `mongo-sharded-server` leaves) and the **0.O.1 Vitess engine-native `file` BackupStorage on NFS + xtrabackup** (VitessAdapter rewritten), plus the platform CA rollover. Earlier **`v0.8.7` (2026-07-06)** — after full-fleet coverage the **completion backlog** closed the last in-CLI gaps (v0.8.6 batch 1 + v0.8.7 batches 2-3): `cluster-status --verbose` timings, Redis/kafka `acl`, the `kafka` meta-cluster delegation; then `scale-up`/`VmrunVmResizer` (vertical CPU/RAM/disk resize + a cluster-safety gate + the honest deb13 root-not-last disk warning), swarm guarded `backup restore --confirm-destructive`, the kafka controller-leader resize-gate, and FoundationAD `backup take` (`ntdsutil ifm`) + FSMO `failover-test`. **`v0.8.5` 2026-06-26 — Phase 0.L.4: the **Registry** adapter (ClusterId `registry`)** — the **fifth and LAST non-data-tier** adapter, **completing full `IClusterAdapter` coverage of the fleet — the CLI now deeply manages EVERY cluster (all 5 non-data tiers + all 13 data/sharded families)**. Manages the **highly-available Harbor** container registry: **2 stateless Harbor app nodes** (round-robin DNS `registry.nexus.lab`) + a **dedicated PostgreSQL/Redis HA datastore pair** (`registry-pg-1/2`) behind a **VRRP VIP** (`registry-db.nexus.lab .119`) + **image blobs in MinIO** (`s3://harbor`, the lakehouse object store) + **Vault as the OIDC SSO provider** (login delegated to Vault → AD). Same SSH-local-curl posture as obs/lakehouse (each node's own ca; Harbor API over node-local HTTPS; PG/Redis/keepalived probed via SSH; KV via `INexusVaultClient`, field `value`); **no managed Harbor/Docker-registry/Npgsql driver** (NetArchTest). Verbs live-verified GREEN + **cold-rebuild-proven**: `status` (2 app nodes + datastore pair + VIP holder) · `health` (both Harbor cores up via RR DNS, PG streaming replication, Redis reachable, MinIO `s3://harbor` blob store reachable, Vault OIDC issuer live, VIP bound) · `topology` · **failover** = registry-db VRRP cutover (keepalived) · `scale-out` = stateless Harbor app node add/remove (the fixed-HA datastore → graceful N/A) · `cert-rotate` (per-node `registry-server` Vault PKI + reload) · `acl` (Harbor projects/robot-accounts + Vault-OIDC group mapping) · `backup` (PG dump round-trip + the MinIO-resident blobs) · `chaos` (process-kill a Harbor app node — the RR-DNS pair tolerates 1). 28.04 MB AOT; 243/243 tests; ADR-0026. **5 of 5 non-data tiers done — full-fleet adapter coverage achieved (all 5 non-data tiers + all 13 data/sharded families).** Preceded by **`v0.8.4` 2026-06-25 — Phase 0.L: the **Lakehouse** adapter (ClusterId `lakehouse`)** — the **fourth non-data-tier** adapter and the last big multi-component one: ONE component-aware adapter spanning the three-engine lakehouse (**MinIO** erasure-coded object store + **Iceberg/Nessie** REST catalog + **Spark** ZK-HA) plus the **ZooKeeper** ensemble across **16 VMs + 1 VRRP VIP**. Same SSH-local-curl posture as obs (each node's own ca; Nessie mgmt `/q/health` + Spark UI `/json/` plain HTTP; MinIO admin via the on-node `mc nexuslocal` alias; KV via `INexusVaultClient`, field `value`); **no managed MinIO/Spark/Iceberg/Nessie driver** (NetArchTest). 11 verbs live-verified GREEN as-is + the full matrix green post-rebuild: `status` (16 nodes + VIP holder + ALIVE Spark master) · `health` (MinIO live/cluster/drives, Nessie object-store + catalog, Spark ALIVE + workers, ZK quorum, iceberg-pg replication, VIP) · `topology` · **`failover --direction spark-master`** (ZooKeeper auto-promotes the STANDBY master, RTO≈31s; `--direction iceberg-pg` = graceful N/A — a VRRP cutover split-brains the catalog DB) · `scale-out` N/A (all roles fixed-size IaC) · `cert-rotate` (vault-agent force-rerender, **MinIO big-bang** + Nessie per-node; Spark/ZK N/A — no rotatable leaf) · `acl` (MinIO policies + users via `mc admin`) · `backup` (`mc mirror s3://warehouse` round-trip) · `chaos` (process-kill a MinIO node — EC tolerates 1). Diagnosed the same v0.8.1-greenfield casualty class as obs (MinIO new-root but Nessie/iceberg-pg/Spark/ZK old-root → cross-tier CA split + iceberg-pg replication split); fixed by a Greg-authorized **cold-rebuild of the Iceberg + Spark envs only** (MinIO kept in place — its EC drives hold 4 cross-tier buckets) + a data-preserving MinIO IAM key re-sync; 2 live-caught adapter bugs fixed. 27.59 MB AOT; 223/223 tests; ADR-0025. **4 of the 5 non-data tiers done.** Preceded by **`v0.8.3` 2026-06-22 — Phase 0.I: the **Observability** adapter (ClusterId `observability`)** — the **third non-data-tier** adapter, managing the **Grafana LGTM stack** (Prometheus + Loki + Grafana + Tempo + Alertmanager + OTel Collector) across **14 VMs + 2 VRRP VIPs** (grafana VIP `.184`, grafana-db VIP `.185`; MinIO — the lakehouse tier — is the Loki/Tempo S3 backend). **Deliberate access divergence, caught by diagnosing the live contract first:** the obs node TLS leaves are on the tier's **OLD CA generation** (the tier was offline during the v0.8.1 Vault greenfield) while the build host now trusts the **NEW root** — so the adapter probes service endpoints **over SSH using each node's own `ca.crt`** + reads runtime creds from Vault KV via `INexusVaultClient` (every observability secret field = `value`); **no managed Prometheus/Grafana/Loki driver** (NetArchTest). **8 verbs live-verified GREEN; zero adapter code bugs:** `status` (14 nodes + VIP holders) · `health` (Prom ready + scrape-targets-up, Alertmanager mesh peers, Loki/Tempo memberlist rings, Grafana database=ok, OTel loopback, Grafana-PG streaming replication, MinIO S3 reachable, both VIPs bound) · `topology` (14 nodes + 2 VIP pseudo-nodes + ring/scrape counts) · **failover = Grafana/Grafana-PG VRRP cutover** (`--direction grafana` proven, **RTO≈1.2s**) · `scale-out` = Loki/Tempo memberlist ring add/remove (the fixed-HA roles → graceful N/A) · `cert-rotate` = build-host `pki_int/observability-server` issue + SSH-push + per-service reload · `acl` = Grafana users via `/api/admin/users` · `backup` = graceful actionable N/A (MinIO erasure-coded + PG repl RPO≈0 + dashboards-as-code + ephemeral Prom TSDB). The verify surfaced **three infra divergences** (all v0.8.1-Vault-greenfield-while-obs-offline casualties, the same class as the swarm tier's Portainer drift): a tier-wide vault-agent broken-trust state (old-root certs vs the new build-host root — this drove the SSH-local-curl posture), the Grafana admin password drifted from KV (`acl` honestly returns HTTP 401 + the reconcile command), and the grafana-pg streaming replication is split (`health` correctly red); cert-rotate / failover grafana-db / acl grant are implemented but not live-run on the degraded tier (they need a Greg-authorized tier trust re-apply first). 27.59 MB AOT (unchanged from v0.8.2); 194/194 tests (+21 parser cases); ADR-0024. **3 of the 5 non-data tiers done; the CLI now manages the data tier + 3 non-data tiers (Foundation + Orchestration + Observability).** Preceded by **`v0.8.2` 2026-06-19 — Phase 0.E: the **Swarm** adapter (ClusterId `swarm`)** — the second **non-data-tier** adapter and the **most reusable**: wires the already-built Consul (`:8501`) / Nomad (`:4646`) / Portainer (`:9443`) clients + `ClusterStatusService` + `FailoverTestService` (built v0.1–v0.5) into the full `IClusterAdapter` surface over the orchestration tier (3 Consul-server/Nomad-server/Swarm-manager nodes + 3 Consul-client/Nomad-client/Swarm-worker/Portainer-agent nodes + a manager-pinned Portainer service). Same build-host control-plane posture (Consul/Nomad mgmt tokens from Vault KV, over HTTPS to a manager IP; **no managed Docker/Consul/Nomad driver** — NetArchTest). `status`/`health`/`topology` = the 3-way rollup enriched with `docker node ls`; failover `--direction` → consul-leader / nomad-leader (RTO≈2–3s) / **swarm-manager** (a vmrun host-level suspend of the raft-leader VM, RTO≈21s); reversible `docker node drain`/`demote` + `nomad node drain` scale-out (quorum-guarded; fixed 3+3 growth = terraform N/A); `consul snapshot save` + `consul kv export` + `nomad operator snapshot save` backup round-trip (restore refused on the live cluster); cert-rotate force-reissues each node's `pki_int` leaves (the vault-agent `pkiCert` persists+reuses the cert, so a bare restart doesn't rotate) then **consul ROLLING / nomad PARALLEL** big-bang; `acl` = Consul + Nomad tokens (bootstrap/agent protected); chaos = `nexus-chaos.sh` on a worker + docker-restart-after-nft. 3 live-caught bugs fixed; **cold-rebuild-proven** (`swarm.ps1 cycle` → smoke 0.E.4e GREEN → full verb matrix GREEN); 27.59 MB AOT; 173/173 tests; ADR-0023. **2 of the 5 non-data tiers done.** Preceded by **`v0.8.1` 2026-06-18 — the first two **non-data-tier** adapters: `VaultAdapter` (ClusterId `vault`) + `FoundationAdAdapter` (ClusterId `foundation-ad`)** — the CLI now manages the **Foundation** tier itself. **VaultAdapter** drives the 3-node raft Vault HA trust root (`vault-1/2/3`) + `vault-transit` (Shamir-seal auto-unseal custodian): control plane = HTTP from the build host with the operator `VAULT_TOKEN` (the root token never ships to a node) + node-local SSH; **no managed Vault driver, no shelled `vault` binary** (NetArchTest). All verbs first-try-green — `status`/`health`/`topology`; failover = `vault operator step-down` on the active (a standby promotes, RTO≈2.0s); scale-out = stop/start a STANDBY raft peer; backup = raft snapshot save + non-destructive `meta.json` inspect (restore deliberately refused on the live trust root); cert-rotate = re-issue listener certs from `pki_int/vault-server` + SIGHUP reload leader-last; acl = Vault policies+approles; chaos = process-kill a STANDBY — **plus a new bespoke `recover-ha` verb** (a new `IRecoverableCluster` capability = the declarative boot-race recovery: unseal `vault-transit` from the Shamir key file → restart `vault-1/2/3` → poll unsealed; the only exposed unseal path). Mutating verbs target STANDBYS so the active stays up. **FoundationAdAdapter** manages the 2-DC `nexus.lab` AD forest (Windows Server 2025: `dc-nexus` + `dc-nexus-2`) over Windows-SSH + the `nexus-gateway` egress over Linux-SSH; `status`/`health`/`topology`/`acl` (AD users/groups) are real, and AD being multi-master, failover/FSMO, DC add/remove, system-state backup, and NTDS cert-rotation/chaos return graceful **actionable "not applicable"** (terraform/out-of-band); `health` proves both DCs reachable + AD replication result=0 + DNS zones AD-integrated + KDS root key present + all 5 FSMO roles + the gateway dnsmasq/nftables/NAT. 1 live-caught bug fixed (the AD-replication probe's explicit `-Server` arg degraded `Get-ADReplicationPartnerMetadata` to empty fields — dropped). No new VMs (targets the existing foundation base + `dc-nexus-2`); 27.36 MB AOT; 159/159 tests; ADR-0022. **Begins the 5 non-data-tier adapters block** (next: v0.8.2 Swarm → v0.8.3 Observability → v0.8.4 Lakehouse → v0.8.5 Harbor). Preceded by **`v0.8.0` 2026-06-18 — the full-fleet roll-up milestone** (all 12 data+sharded adapter families sealed + aggregate AOT re-validated ≤30 MB, no new adapter). Earlier: **`v0.7.3` 2026-06-18 — Phase 0.P: the **Citus** adapter** (ClusterId `citus`) — the third adapter on the 0.7.x sharded line, closing the relational-PostgreSQL-sharding axis: a **Citus-sharded PostgreSQL cluster with full Patroni HA** (3 etcd DCS + a coordinator Patroni pair @ VIP `.211` = `pg_dist_node` groupid 0 + 2 worker Patroni pairs @ VIPs `.212`/`.213` = groupid 1/2; PG 17 + Citus 14.1; `events` hash-distributed on `tenant_id`, 32 shards = 16+16 across the worker groups, + colocated `event_tags` + reference `tenants`). **Citus = Patroni HA per group + Citus distribution** — reuses the PatroniAdapter HA idioms three times + the Vitess populated-Shards topology. Operator = the dedicated `nexus-cluster-admin` role (ADR-0011 Vault-KV model, `nexus/citus/operator-password`) auto-propagated to the workers with a `~postgres/.pgpass` entry so **distributed queries run as the operator** via the coordinator VIP (mTLS+scram+client-cert); SSH-shell-out to `patronictl`/`psql`/`etcdctl` (no managed Npgsql driver). `topology` **populates Shards** (per-worker-group placements); failover = graceful `patronictl switchover` on a chosen group (RTO≈1.6s; the VRRP VIP follows the leader so `pg_dist_node` is untouched); scale-out = Patroni-member add/remove (worker-group growth = apply-on-demand `citus_add_node`+`rebalance_table_shards`); backup = operator `COPY … TO STDOUT` round-trip of the distributed dataset (800 events); cert-rotate = per-node Vault PKI (PG reload / etcd restart, coord-leader-last); `acl` = PG roles via the operator (`CREATE ROLE` propagates to workers); chaos = process-kill `nexus-patroni`. Adapter code first-try-green on every verb (the one live-caught issue was infra — the patroni.yml `ctl:` block, the 0.G.4 lesson — fixed + baked); 26.71 MB AOT; 137/137 tests. **12 of 13 adapter families live.** Earlier: **`v0.7.2` 2026-06-17 — Phase 0.O: the **Vitess** adapter** (ClusterId `vitess`) — the second adapter on the 0.7.x sharded line: the Vitess-sharded MySQL/Percona cluster (3 etcd topo + vtctld/VTOrc control + 2 vtgate routers + 2 shards ×3 tablets; keyspace `commerce` split `-80`/`80-` by a **hash vindex on `customer_id`**; Percona Server 8.4 under `mysqlctld`). Full verb surface over a **hybrid operator identity** — the mTLS gRPC control plane via `sudo nexus-vtctldclient` (no password) + the SQL plane via the vtgate `:15306` mTLS listener as static-auth user `nexus` (password in Vault KV `nexus/vitess/mysql-app-password`, the ADR-0011 model) — by SSH-shell-out to `vtctldclient`/`mysql`/`mysqldump` (no managed driver). Tablets register by VMnet10 IP (mapped via vms.yaml); primaries read from the topo (they drift off the lowest uid). `topology` **populates Shards** (hash-vindex key ranges); failover = graceful `PlannedReparentShard` (RTO≈0.17s); **backup = logical `mysqldump` per shard** round-trip (101 rows — no BackupStorage configured in 0.O, so engine-native Backup is the 0.O.1 enhancement); cert-rotate = per-node Vault PKI (vttablet-only restart, mysqld stays up); `acl` = the **vtgate static-auth file** (`vtgate_creds.json` — vtgate doesn't proxy `CREATE USER`); chaos = SIGSTOP a tablet — a primary freeze triggers **VTOrc auto-reparent** (proven live). Live-verified GREEN, 3 bugs fixed; 26.52 MB AOT; 114/114 tests. **11 of 13 adapter families live.** Earlier: **`v0.7.1` 2026-06-16 — Phase 0.N: the **MongoSharded** adapter** (ClusterId `mongo-sharded`) — the first adapter on the 0.7.x line and the genuinely-**sharded** MongoDB cluster, distinct from the 0.G.2 `mongo` replica set. Full verb surface over the 11-node topology (3 config-server RS @ 27019 + 2 shard RSes ×3 @ 27018 + 2 `mongos` routers @ 27017) via SSH-shell-out to `mongosh`/`mongodump`/`mongorestore` (no managed driver). **Two-headed keyFile auth** (both using the shared keyFile content as the password): `__system`@`local` for direct mongod RS ops (config + shards — the only principal the shard mongods accept) + `nexus-sharded-admin`@`admin` **through a mongos** for cluster-level ops (`local` can't be used through mongos). `topology` **populates Shards** (the sharded showcase); shard-primary failover RTO≈2.8s; `mongodump`-through-mongos backup round-trip (200 docs); `cert-rotate` = graceful N/A (no TLS in 0.N v1; mTLS is the deferred 0.N.1 hardening). Live-verified GREEN, 1 bug fixed; 26.30 MB AOT; 97/97 tests. **10 of 13 adapter families live.** Preceded by **`v0.7.0` 2026-06-16 — the Phase 0.G base roll-up** (the exit-gate milestone: the 9 data-tier adapter families sealed + aggregate AOT validated ≤30 MB, no new adapter). Earlier: **`v0.6.7` 2026-06-15 — Phase 0.H.7: the **Kafka** adapters** — promoted the v0.5 failover-only retrofit to the full verb surface. One parameterized `KafkaClusterAdapter` registered **twice** — `kafka-east` + `kafka-west` (3 combined broker+controller KRaft nodes each) — plus a `KafkaEcosystemAdapter` (`kafka-ecosystem`: Schema Registry, REST, Connect, ksqlDB, MirrorMaker 2); the v0.5 `kafka` adapter stays as the cross-region MM2 DR meta-cluster. **mTLS-only — no operator password, no Vault client** (like Redis): the operator identity is the broker's own Vault-PKI keystore via `sudo kafka-*.sh --command-config`; **no managed `Confluent.Kafka`** (NetArchTest). Verbs live-verified GREEN against **both** clusters + the ecosystem: status · health (quorum + voter lag + 0 under-replicated) · topology (topics × partitions/RF/ISR) · failover (**controller-leader move** via `kafka-metadata-quorum`, RTO≈4.5s — complements the cross-region MM2 DR) · scale-out (broker drain/rejoin) · backup (topic→`.jsonl`→verify-topic produce/consume round-trip) · cert-rotate (rolling reissue from each node's own Vault token + `kafka-tls-split.sh`) · **acl** (enabled the KRaft `StandardAuthorizer` via a new cold-rebuild-proven overlay — `super.users` = all 15 platform principals so ordinary app principals stay deny-by-default) · chaos. **Zero live-caught bugs** (the up-front contract probe pre-empted them). 26.18 MB win-x64 AOT (≤30 MB); 86/86 tests. **9 of 13 adapter families live.** Earlier: **`v0.6.6` 2026-06-12 — Phase 0.G.7: the SQL Server **FCI** + **Always On AG** adapters — the first **Windows** cluster, shipped as TWO adapters over one vms.yaml cluster (`sqlserver` = WSFC/FCI + `sqlserver-ag` = AG/Listener). Access pattern fundamentally different from the six Linux adapters: Windows-SSH (`powershell -EncodedCommand`) + on-node `sqlcmd`, **no managed `Microsoft.Data.SqlClient`** (NetArchTest). Two planes: WSFC/cluster cmdlets (`Move-ClusterGroup`, `Get-Cluster*`) over plain SSH as local nexusadmin; FCI T-SQL as the `nexus-cluster-admin` SQL login (the Vault-KV operator-credential model). FCI verbs — status · health (NodeMajority quorum + clustered Physical Disk + iSCSI + operator-auth) · topology · failover (**`Move-ClusterGroup`**, RTO≈4.5s) · backup (COPY_ONLY round-trip, 8225 rows) · cert-rotate (**one shared cert + single cluster checkpoint**) · acl · chaos. AG verbs — status · health (**Listener strict-TLS** `-N` → `PRIMARY=SQLFCI`) · topology · failover (**`ALTER AVAILABILITY GROUP FAILOVER`**, RTO≈8.2s) · scale-out remove/add (**manual seeding** — automatic seeding fails: FCI files on shared `S:\`, replicas on local `C:\`) · backup · cert-rotate (per-node) · acl · chaos. One live-caught bug fixed (the automatic-seeding scale-out). 25.95 MB win-x64 AOT (≤30 MB); 71/71 tests; smoke-0.G.7 56/56. Earlier: v0.6.5 Phase 0.G.6 — the StarRocks adapter — the second **analytics-tier** engine, an MPP MySQL-protocol warehouse (3 FE **BDB-JE metadata quorum** + 3 BE tablet nodes; `DISTRIBUTED BY HASH BUCKETS` × `replication_num=3`) — live-verified end-to-end against the running `starrocks` cluster (all data-tier verbs green — `status`/`topology` (`SHOW FRONTENDS`/`SHOW BACKENDS`, dynamic FE leader, VMnet10-backplane IP mapping) · `health` (fe-quorum + operator-auth + per-BE TabletNum + distributed-query) · `failover` (**FE leader re-election**, BDB-JE) RTO≈1.5s · `backup take/restore` (genuine async `BACKUP/RESTORE SNAPSHOT` to the file:// NFS repo, polled to FINISHED, 60 rows) · `cert-rotate` (6 nodes, PKCS#8, BE-first/FE-leader-last) · `acl list/grant` (`SHOW USERS` + `SHOW GRANTS` + `CREATE USER`) · `scale-out add/remove` (BE leave/rejoin) · `chaos` (process-kill + rejoin)). Auth model decided from a live probe: password-auth (root requires a password over the MySQL wire) → reuses the v0.6.1 Vault-KV operator-credential model verbatim (`nexus-cluster-admin` granted cluster_admin/db_admin/user_admin, password only in Vault KV via the optional `INexusVaultClient`); connection via `mysql --skip-ssl` + `MYSQL_PWD`. First-try-green on all 12 verbs. 8 of 13 adapters live (Redis v0.6.0 mTLS-only + Mongo v0.6.1 + Percona v0.6.2 + Patroni v0.6.3 + ClickHouse v0.6.4 + StarRocks v0.6.5 + SQL Server FCI v0.6.6 + SQL Server AG v0.6.6 — the first Windows cluster); remaining per canon order (full-Kafka → mongo-sharded → Vitess → Citus → the 5 non-data tiers). Earlier: v0.6.4 ClickHouse adapter (first sharded — 3 shards × 2 replicas + ClickHouse Keeper RAFT, Keeper-leader failover RTO≈1.1s, native BACKUP/RESTORE), v0.6.3 PostgreSQL Patroni HA adapter (etcd DCS + HAProxy VIP, `patronictl switchover` RTO≈4.6s), v0.6.2 Percona XtraDB Cluster + ProxySQL adapter (Galera multi-primary, ProxySQL writer failover RTO≈2.3s), v0.6.1 Mongo replica-set adapter, v0.6.0 `IClusterAdapter` SPI + Redis adapter.** **`v0.5.0` released 2026-05-15 — Phase 0.F closed; all 5 of 5 master-plan verbs live.** `kafka failover {east-to-west, west-to-east}` ships (ADR-0008: demo-grade DR via vmrun-suspend × 3 source brokers + RF=3 produce/consume round-trip on the target, vmrun-resume; **live RTOs 13.20 s + 13.57 s** under the 60 s master-plan gate). Earlier: **v0.4.0** `demo {list, run, record}` (JSON-spec orchestrator + VHS `.tape` recorder); **v0.3.x** `failover-test {consul-leader, nomad-leader, swarm-manager}` (live RTOs 1.55 s / 2.716 s / 21.59 s, ADR-0007 SSH.NET); **v0.2.x** `infrastructure {list, status, suspend, resume}` (ADR-0006 hand-rolled `vms.yaml` reader); **v0.1.0 alpha** `cluster-status`. 22.75 MB win-x64 AOT binary, 2.25 MB headroom against the 25 MB exit gate. Verbs / RTOs all verified live end-to-end. |
| ⚪ | `nexus-shared` | Shared .NET 10 libraries — OTel bootstrap, Vault client, Problem Details, Outbox helpers, auth primitives |
| 🟢 | [`nexus-infra-vmware`](https://github.com/grezap/nexus-infra-vmware) | Tier-1 foundation + the cross-tier IaC every other tier consumes. Packer templates (Debian 13, Ubuntu 24.04, Windows Server 2025 core/desktop, Windows 11 Enterprise, Vault) + Terraform `foundation` env (AD DS forest + domain-joined jumpbox + AD hardening + GMSA + Vault Agent on Windows + NFSv4/iSCSI exports from the gateway + **dhcp-host reservations, round-robin DNS, and VRRP-VIP records for every tier**) + `security` env (**3-node HA Vault on Raft with transit auto-unseal + PKI hierarchy 90-day leaves + LDAPS auth/ldap + secrets/ldap rotation**, and the per-tier Vault-side scaffolding consumed by every cluster: PKI roles + per-host AppRole sidecars + sticky-seeded KV creds for swarm · kafka · oltp (redis/mongo/percona/patroni/sqlserver) · analytics (clickhouse/starrocks) · lakehouse (minio/iceberg/spark) · registry, plus the **Vault OIDC provider** for Harbor SSO [0.L.4]). **Phase 0.D fully closed; live + cold-rebuildable**, foundation + security smoke ALL GREEN. The always-on 6-VM base (gateway + dc-nexus + vault-1/2/3 + vault-transit) underpins the whole lab. |
| 🟢 | [`nexus-infra-swarm-nomad`](https://github.com/grezap/nexus-infra-swarm-nomad) | Tier-2 orchestration — Docker Swarm (3+3) + Nomad + Consul + Portainer CE + Vault Agent on every node. **`v0.2.0` released 2026-05-08 — Phase 0.E fully closed, cold-rebuildable.** All 6 sub-phases sealed (`0.E.1`/`2.1-3`/`3.1-3`/`4`/`4e`/`5`); cold-rebuild proven end-to-end via `terraform destroy` → `packer build` → `terraform apply` → 409-check smoke gate ALL GREEN with stock root-only CA bundle on build host. Earlier: **Phase 0.E.3.3 ✅** (~155-check chained smoke green: 0.E.1 swarm cluster + 0.E.2.1 Consul gossip + 0.E.2.2 Consul TLS + 0.E.2.3 Consul ACL deny-mode + 0.E.3.1 Nomad TLS (mTLS RPC + raft + HTTPS:4646, `verify_server_hostname=true`) + 0.E.3.2 Nomad ACL (mgmt token in Vault KV, 6 per-host operator tokens, anon-deny enforced) + **0.E.3.3a Nomad → Consul HTTPS rewire** (Nomad agents talk Consul over `https://127.0.0.1:8501` with per-host ACL token rendered by Vault Agent; legacy `consul { address = "127.0.0.1:8500" }` block surgical-removed from `nomad.hcl`) + **0.E.3.3b Nomad-Vault integration**)). **Phase 0.E.4 + 0.E.4e ✅ closed** (Portainer CE clustered Swarm service + TLS full-chain on the wire + `inet filter forward` ingress-mesh accept rules + cold-rebuild proof: `terraform destroy` → `packer build` → `terraform apply` → smoke ALL GREEN with stock root-only CA bundle on the build host). **v0.1.1 tagged 2026-05-08.** |
| 🟢 | [`nexus-infra-kafka`](https://github.com/grezap/nexus-infra-kafka) | Tier-3 data backbone — the Kafka ecosystem. **`v0.1.0` released 2026-05-15 — Phase 0.H complete, all 6 sub-phases closed, cold-rebuildable.** 15 VMs: two 3-node KRaft clusters (`kafka-east` primary + `kafka-west` DR) on **mutual TLS**, a Schema Registry HA pair, a Confluent REST Proxy, a Kafka Connect distributed cluster (Debezium Postgres + SQL Server plugins), a ksqlDB cluster, and a **MirrorMaker 2 cross-cluster DR pair** — every node holds a per-node Vault-PKI keystore and talks to the brokers over mTLS. The **Phase 0.H exit gate** is cleared: a record produced to `kafka-east` appears on the mirrored topic on `kafka-west` via MirrorMaker 2, and the reverse. Cold-rebuild proven end-to-end (`destroy` → `security apply` → `apply` → smoke `0.H.2`-`0.H.5` ALL GREEN, no operator hot-state). Sub-phases: **`0.H.1`** `kafka-node` Packer template (Temurin JDK 21 + Apache Kafka 3.8.1 + Confluent Community 7.7.1) + both KRaft clusters (38 checks); **`0.H.2`** broker mutual TLS (92); **`0.H.3`** Schema Registry HA pair + REST Proxy (37); **`0.H.4`** Kafka Connect + Debezium + ksqlDB (48); **`0.H.5`** MirrorMaker 2 + the phase exit gate (38); **`0.H.6`** close-out canon batch + cold-rebuild proof. |
| 🟢 | [`nexus-infra-oltp`](https://github.com/grezap/nexus-infra-oltp) | Tier-4 OLTP data backbone — **OLTP TIER 6/6 clusters cold-rebuild proven** (5/5 SEALED 2026-05-22 + **Phase 0.N MongoDB sharded cluster SEALED 2026-05-30**). **Phase 0.N (ADR-0040)** adds an 11-VM **sharded** cluster (distinct from the 0.G.2 replica set): 3 config-server RS (`mongo-cfg-1/2/3`, port 27019) + 2 shard RSes × 3 (`shard-1`/`shard-2`, port 27018) + 2 stateless `mongos` routers (round-robin DNS `mongos.nexus.lab`) — the document-store **sharding** showcase: a hashed-shard-key collection's chunks span both shards, `mongos` routes keyed lookups, each shard is its own 3-node RS. keyFile internal auth; clients auth `nexus-sharded-admin`@admin (mongos forbids the `local` DB); **0.N.1 wire mTLS DONE** — `net.tls.mode requireTLS` + per-host `mongo-sharded-server` Vault-PKI leaves + online `rotateCertificates`. Live-ratified + cold-rebuild-proven (`smoke-0.N.ps1` **50/50** both times; 10 transients fixed in source, handbook §3.N/§3.Nb). Phases 0.G.1 + 0.G.2 + 0.G.3 + 0.G.3.5 + 0.G.4 closed 2026-05-18 → 2026-05-19 (4 Linux data clusters proven end-to-end). **Phase 0.G.7 (SQL Server FCI + Always On AG on Windows Server 2025) LIVE-RATIFIED 2026-05-22 — `smoke-0.G.7.ps1` ALL GREEN 56/56** adds the 5th cluster + 4 ws2025-desktop VMs: a 2-node WSFC Failover Cluster Instance (virtual name `sqlfci` @ `.16`, sharing an iSCSI LUN from `nexus-gateway` as a clustered Physical Disk per ADR-0026) + 2 async AG replicas (`nexus-ag`, both SYNCHRONIZING+HEALTHY, MANUAL-seeded since the FCI keeps DBs on shared `S:\` vs replicas on local `C:\`) + 3 WSFC-managed VIPs (cluster `.15`, FCI `.16`, AG Listener `.17`). SQL service identity = `nexus.lab\gmsa-sql-engine$` on the FCI (first real GMSA consumer; replicas run as NETWORK SERVICE); AG endpoint authentication = certificate-based per ADR-0027. Per ADR-0025 the AG Listener IS the LB-tier HA primitive — a remote domain client `sqlcmd -E -N` (Encrypt + strict cert-chain validate) reaches the primary across the listener (cert IP-SAN .17 + Kerberos SPNs on the gmsa). Engines live: **6-node Redis 8.0 Cluster** (3 masters + 3 replicas, TLS-only on 6379/16379 with Vault-PKI per-node leaf certs, cluster bus on VMnet10 backplane), **3-node MongoDB 8.0 Replica Set `nexus-rs`** (mTLS on 27017 + keyFile internal auth + SCRAM-SHA-256 smoke-rw user), **3-node Percona XtraDB Cluster 8.0.45 + 2-node ProxySQL 2.6** (mTLS-only on 3306 via per-host Vault PKI, Galera SST/IST encrypted over VMnet10 via `pxc-encrypt-cluster-traffic=ON`, ProxySQL `galera_hostgroups` with writer/backup_writer/reader splits, VRRP-floated VIP `192.168.70.50` between proxysql-1 MASTER + proxysql-2 BACKUP using unicast mode). Also live: **3-node Patroni 4.0.5 PostgreSQL 17 HA cluster** (`pg-primary` + `pg-replica-1` + `pg-replica-2`) + **3-node etcd 3.5.16 raft quorum** (Patroni DCS at `/service/nexus-pg/` with HTTP basic-auth RBAC, mTLS for wire encryption) + **2-node HAProxy 3.0 LTS HA pair** (`haproxy-pg-1` + `haproxy-pg-2`) with **VRRP-floated VIP `192.168.70.60`** routing `:5432` to current Patroni leader via REST `/leader` health probes (mirrors the 0.G.3 proxysql HA pair pattern; no SPOF on the LB tier). All cluster smoke gates ALL GREEN; 0.G.4 smoke gate 152/152 ALL GREEN. **0.G.3.5 was a mid-phase architectural refactor** — the monolithic `packer/oltp-node/` + `terraform/envs/oltp/` had a 30-min iteration loop that stalled the 0.G.3 ratification at 16 transients; the refactor split into per-engine templates + per-cluster states + per-cluster nftables overlays + per-cluster operator scripts, shrinking iteration to ~5-10 min per cluster. 27 distinct transients root-caused + permanently fixed across both ratifications. Architectural rule canonized: multi-cluster infra tiers default to per-cluster state + per-engine template (now applied across 0.G.4 → 0.G.7). 0.G.7 ratification surfaced 40+ transients all fixed in source (handbook §3.5b + §3.5c). **nexus-cli `SqlFciAdapter` + `SqlAgAdapter` LIVE (v0.6.6, 2026-06-12)** — both planes (`sqlserver` FCI + `sqlserver-ag` AG) verb-complete + live-verified + 17 System B demos; the operator login `nexus-cluster-admin` seeded via `role-overlay-sqlserver-operator-login.tf`. |
| 🟢 | [`nexus-infra-analytics`](https://github.com/grezap/nexus-infra-analytics) | Tier-4 analytics data backbone — ClickHouse + StarRocks shared-nothing + StarRocks shared-data (20 VMs on `04-analytics`). **Phases 0.G.5 + 0.G.6 SEALED 2026-05-23 (`v0.1.0`)** + **Phase 0.L.5 SEALED 2026-05-26 (`v0.2.0`)** — all three clusters live-ratified + cold-rebuild-proven (`smoke-0.G.5.ps1` **129/129** · `smoke-0.G.6.ps1` **73/73** · `smoke-0.L.5.ps1` **69/69** GREEN with chaos default-on; StarRocks 3.5.17 on JDK 21; 20 apply-time transients fixed in source). **Phase 0.L.5 StarRocks shared-data (ADR-0037)** — second StarRocks cluster (parallel to the sealed shared-nothing one): **3 FE BDB-JE quorum (`sr-sd-fe-1/2/3` at `.37`/`.38`/`.39`) + 2 stateless Compute Nodes (`sr-sd-cn-1/2` at `.30`/`.40` — decade-spill)** on `run_mode=shared_data`; internal cloud-native tables in a MinIO storage volume `nexus_minio_starrocks` → `s3://starrocks/` (dedicated `nexus-starrocks-app` service account + scoped `starrocks-tenant` MinIO policy, cross-bucket-deny proven). Headline HA property — any CN serves any query from shared MinIO storage — proven by chaos every smoke run. **ClickHouse (0.G.5)**: a genuine **3 shards × 2 replicas** cluster (6 data nodes) + a dedicated **3-node ClickHouse Keeper** RAFT quorum (NOT ZooKeeper — the lab runs zero ZooKeeper VMs) — `Distributed` tables over `ReplicatedMergeTree` with `internal_replication=true` + per-node `{shard}`/`{replica}` macros; mTLS-only (secure ports 8443/9440/9010 + Keeper 9281) via per-host Vault PKI (PKCS#8). **StarRocks shared-nothing (0.G.6)**: a **3 FE (BDB-JE quorum: 1 leader + 2 followers) + 3 BE** cluster — tables `DISTRIBUTED BY HASH(...) BUCKETS n` with `replication_num=3` so tablets shard across all 3 BE; MySQL protocol on `:9030`. **Client front door = round-robin DNS** (`clickhouse.nexus.lab` / `starrocks-fe.nexus.lab` / `starrocks-sd-fe.nexus.lab`), **no VRRP VIP** — all engines are any-node-addressable so there's no fixed-endpoint SPOF (resolves ADR-0025's analytics open question in ADR-0031). Backup repository (0.G clusters) = NFS export from `nexus-gateway` (ADR-0032); shared-data uses the MinIO storage volume itself. Per-engine Packer templates (`analytics-clickhouse-{keeper,server}-node`, `analytics-starrocks-{fe,be}-node`, `analytics-starrocks-sd-{fe,cn}-node`) + per-cluster Terraform states + per-cluster smoke gates (`smoke-0.G.5.ps1` / `smoke-0.G.6.ps1` / `smoke-0.L.5.ps1`) + 22+ System B demos + System A `DEMO-15` (sn) + `DEMO-18` (sd) + ADRs 0028–0032 + 0037. RAM right-sized for the lab (logged in `vms.yaml`). |
| 🟢 | [`nexus-infra-lakehouse`](https://github.com/grezap/nexus-infra-lakehouse) | Lakehouse tier (`08-spark`, Phase 0.L) — MinIO (S3 object store) + Apache Iceberg (REST catalog + dedicated PG HA) + Apache Spark. **0.L.1 MinIO SEALED 2026-05-23** — a **4-node distributed erasure-coded** cluster (each node a dedicated xfs data drive, default EC:2, tolerates a node loss), mTLS via per-host Vault PKI, client front door = round-robin DNS `minio.nexus.lab` (no VIP, ADR-0033). Live-ratified + **cold-rebuild proven** (`smoke-0.L.1.ps1` 41/41 GREEN; 2 apply-time transients fixed in source — multi-disk preseed + the VMnet10 NO-CARRIER auto-recovery). **0.L.2 Iceberg catalog SEALED 2026-05-24** — **Project Nessie** ×2 stateless instances (round-robin DNS `iceberg.nexus.lab`, Iceberg REST API on HTTPS `:19120`) backed by a **dedicated PostgreSQL 17 master-replica HA pair** (streaming replication + **keepalived VRRP VIP** `iceberg-db.nexus.lab .151` with auto-promotion) — making the lakehouse self-contained; warehouse data in MinIO via a secret-URN S3 credential. Live-ratified + **cold-rebuild proven** (`smoke-0.L.2.ps1` 28/28 GREEN; 8 apply-time transients fixed in source, handbook §3.4; ADR-0034). **0.L.3 Spark SEALED 2026-05-24** — Apache Spark 3.5.3 standalone **HA: 2 masters (ZooKeeper-elected, active/standby) + 3 workers** + a dedicated **3-node Apache ZooKeeper** ensemble (the platform's one deliberate Apache-ZK exception — Kafka is KRaft, ClickHouse uses Keeper; `recoveryMode=ZOOKEEPER`, no master VIP). Spark writes Iceberg tables through the Nessie REST catalog (warehouse-by-name + `S3FileIO`/AWS SDK v2) into the MinIO warehouse — the end-to-end Spark→Iceberg→MinIO write path 0.L.1/0.L.2 deferred. Live-ratified + **cold-rebuild proven** (`smoke-0.L.3.ps1` 28/28 GREEN; 10 apply-time transients fixed in source incl. the executor-RPC firewall gap, handbook §3.6; ADR-0035). Lakehouse tier 16 VMs. **Tagged `v0.1.0` 2026-05-26** as part of the 0.L close-out (all 3 lakehouse sub-phases sealed; ADRs 0033-0035). The lakehouse-minio env also hosts the dedicated MinIO tenant for the 0.L.5 SR-shared-data cluster (bucket `starrocks` + scoped `starrocks-tenant` policy with cross-bucket-deny proven; ADR-0037). |
| ⚪ | `nexus-infra-k8s` | Tier-3 orchestration — kubeadm HA control plane, Cilium CNI, cert-manager, Argo CD, Kyverno policy |
| 🟢 `v0.1.0` | `nexus-infra-observability` | Observability tier (`01-foundation` extension, Phase 0.I) — **Grafana LGTM stack** with full HA across the LB tier per ADR-0025. **Phase 0.I COMPLETE 2026-05-28 -- tagged `v0.1.0`.** All 7 sub-phases SEALED (live-ratified + cold-rebuild-proven). **14 VMs + 2 VRRP VIPs**: Prom HA `prom-1/2` + Alertmanager mesh co-resident (0.I.1) · Loki simple-scalable `loki-1/2/3` on MinIO bucket `loki` (0.I.2) · Tempo scalable `tempo-1/2/3` on MinIO bucket `tempo` (0.I.3) · **Grafana HA `grafana-1/2` behind VRRP VIP `grafana.nexus.lab .184` active-active over a shared dedicated PG HA pair `grafana-pg-1/2` behind VRRP VIP `grafana-db.nexus.lab .185` (0.I.4)** · **OTel Collector pair `otel-collector-1/2` fronted by RR DNS `otel.nexus.lab` (no VIP per ADR-0031); OTLP gRPC :4317 + HTTP :4318 receivers routing traces -> Tempo, metrics -> Prom remote-write, logs -> Loki native OTLP (0.I.5)** · **Vector log shipper added to the shared `nexus_observability` ansible role + `scripts/fleet-vector-rollout.ps1` for live retrofit -- every fleet Linux node ships journald + /var/log/* to Loki HA pair tagged `fleet=nexusplatform` (0.I.6)**. mTLS via per-host `observability-server` Vault PKI (cert IP-SAN includes the VIP -> `sslmode=verify-full` validates against the floating VIP). **39 apply-time transients permanently fixed in source** (handbook §3.A T1-T8 / §3.B T9-T14 / §3.C T15-T19 / §3.D T20-T29 / §3.E T30-T33 / §3.F T34-T37). |
| 🟢 | [`nexus-infra-registry`](https://github.com/grezap/nexus-infra-registry) | Registry/platform tier (`09-platform`, Phase 0.L.4) — a **highly-available Harbor** container registry. **SEALED 2026-05-25** — live-ratified + **cold-rebuild proven** (`smoke-0.L.4.ps1` 41/41 GREEN; 7 apply-time transients fixed in source, handbook §3.2; ADR-0036). **HA app tier, no SPOF**: 2 stateless Harbor nodes (`registry-1/2`) behind **round-robin DNS** `registry.nexus.lab` (ADR-0031) — all state is external, so either node can be lost and push/pull keep working. **Image blobs live in MinIO S3** (the 0.L.1 object store — the registry is the first non-lakehouse S3 consumer), not local disk. **HA datastore**: a dedicated **PostgreSQL 17 master-replica pair + co-located Redis** (`registry-pg-1/2`) with a **keepalived VRRP VIP** `registry-db.nexus.lab .119` (the proven 0.L.2 pattern) — Harbor's metadata DB + cache. **Supply chain**: **Trivy** scans every push for CVEs; images are **cosign**-signed and Harbor surfaces the signature. **SSO**: login delegated to **Vault as an OIDC provider** (`auth_mode=oidc_auth`) which authenticates against AD via its existing `auth/ldap`; a local admin is the break-glass account. mTLS via per-host Vault PKI (`registry-server` role); Harbor 2.14.4 deployed via its offline docker-compose installer (images baked, zero apply-time egress). 4 VMs + 1 VIP. |
| 🟢 `v0.1.0` | [`nexus-infra-vitess`](https://github.com/grezap/nexus-infra-vitess) | Vitess-sharded MySQL tier (`07-vitess`, Phase 0.O) — the **relational (MySQL) horizontal-sharding** showcase, distinct from PXC/Galera *synchronous replication* (0.G.3). **SEALED 2026-06-03 (`v0.1.0`, ADR-0041)** — live-ratified + **cold-rebuild proven** (`smoke-0.O.ps1` **71/71 GREEN** both times; transients fixed in source). 12 VMs: a **3-node etcd 3.5.16** topology server + a control node (`vitess-control-1`) running **vtctld** (admin/DDL) + **VTOrc** (automated failover) + **2 stateless vtgate routers** (`vitess-vtgate-1/2`, round-robin DNS `vtgate.nexus.lab`, MySQL protocol on `:15306`, no VIP per ADR-0031 — the client front door) + **2 shards × 3 tablets** (`vitess-shard{1,2}-tablet-{1,2,3}`), each tablet a **vttablet sidecar + Percona Server 8.4 LTS mysqld** pair. Keyspace `commerce`, 2 shards (`-80`/`80-`) on a **hash vindex** — one vtgate insert of 100 rows splits 53/47 across both shards; **VTOrc auto-reparents** a shard (~15s) when its PRIMARY is killed and the cluster stays writable. **Vitess v24.0.1**; **full Vault-PKI mTLS** on every gRPC channel (vtgate↔vttablet, vtctld↔vttablet, VTOrc↔vttablet, *↔etcd) + the mysqld wire + the vtgate MySQL listener. Intra-cluster gRPC + MySQL replication ride the VMnet10 backplane. System A `DEMO-21` + System B `demo-0.O-*` JSON demos; nexus-cli `VitessAdapter` LIVE (v0.7.2) + **0.O.1 engine-native `file` BackupStorage on NFS + xtrabackup** (adapter rewritten). |
| 🟢 `v0.1.0` | [`nexus-infra-citus`](https://github.com/grezap/nexus-infra-citus) | Citus-sharded PostgreSQL tier (`08-citus`, Phase 0.P) — the **relational (PostgreSQL) horizontal-sharding** showcase with **full Patroni HA**, distinct from Patroni *streaming replication* (0.G.4). **SEALED 2026-06-03 (`v0.1.0`, ADR-0042)** — live-ratified + **cold-rebuild proven** (`smoke-0.P.ps1` **69/69 GREEN** both times incl worker Patroni failover; transients fixed in source). 9 VMs + 3 VRRP VIPs: a **3-node etcd 3.5 DCS** (Patroni quorum) + a **coordinator Patroni pair** (`citus-coord-1/2`, VIP `coord.citus.nexus.lab`) holding the `pg_dist_*` catalog + **2 worker Patroni pairs** (`citus-worker{1,2}-*`, VIPs `worker{1,2}.citus.nexus.lab`) holding the shards. **PostgreSQL 17 + Citus 14.x** (`shared_preload_libraries='citus'`); every node-group is a 2-node Patroni cluster fronted by a **keepalived VRRP VIP that follows the Patroni leader** (workers registered in `pg_dist_node` **by VIP** so a failover needs no metadata rewrite). A distributed `events` table (32 shards) spreads across both worker groups; a cross-shard aggregate is routed/merged by the coordinator; a reference table + colocated join + worker-Patroni-failover (VIP follows new leader, cross-shard query continues) all proven. **Full Vault-PKI mTLS** on the PG wire + etcd + Patroni REST (`clientcert=verify-ca`); intra-cluster traffic rides the VMnet10 backplane. System A `DEMO-22` + System B `DEMO-27/28/29` JSON demos; nexus-cli `CitusAdapter` LIVE (v0.7.3). |
| 🟢 | [`nexus-infra-platform-tools`](https://github.com/grezap/nexus-infra-platform-tools) | Platform-tools tier (`09-platform`, Phase 0.Q) — self-hosted developer/data-platform services alongside the data plane. **Phase 0.Q.1 Marquez / OpenLineage SEALED 2026-07-21 (ADR-0043)** — live-ratified + **cold-rebuild proven** (`smoke-0.Q.1.ps1` ALL GREEN both times; from-zero re-apply came up with **zero transients**; 5 first-build transients fixed in source, handbook §3.2). **Marquez** is the OpenLineage lineage backend — pipelines emit run/job/dataset events and it renders the "if I change this table, what breaks downstream?" graph (enhancement E16; first emitter is `dataflow-studio`). Deployed as **Docker Compose on the VM** (`marquez` `.127`: API `:5000` + admin `:5001` + web `:3000` behind an **nginx TLS front door** on `:443` carrying the node's Vault-PKI leaf) on a **dedicated PostgreSQL 17 HA pair** (`marquez-pg-1/2` `.134`/`.135`, streaming replication + **keepalived VRRP VIP** `marquez-db.nexus.lab .136`) — dedicated rather than co-located so the lineage plane's availability isn't coupled to the image registry's. A 2-job / 3-dataset lineage graph (`raw.orders → curate-orders → curated.orders → load-dwh → dwh.fact_order`) is emitted + read back live by the committed `scripts/marquez-lineage-demo.ps1`; PG TLS is `verify-full` **through the VIP** (both PG leaves carry the VIP DNS+IP SANs). 3 VMs + 1 VIP; `.125`/`.126`/`.128` reserved for future Prefect (0.Q.2) / Unleash (0.Q.3) / Backstage (0.Q.4). |
| 🟢 | [`local-data-stack`](https://github.com/grezap/local-data-stack) | Reproducible single-host Compose stack (Kafka KRaft, ClickHouse, Redis, OTel, Prometheus, Grafana, Jaeger, Seq). [v0.1.0 released](https://github.com/grezap/local-data-stack/releases/tag/v0.1.0). |

### Application projects

| # | Status | Project | Architecture | One-line pitch |
|---:|:---:|---|---|---|
| 0 | ⚪ | `portfolio` | Clean Arch | This website — Blazor Server portfolio homepage |
| 1 | 🟡 | [`dataflow-studio`](https://github.com/grezap/dataflow-studio) | Modular Monolith | SQL Server CDC → Kafka → StarRocks DWH + ClickHouse analytics. **The pipeline runs end-to-end on the lab (2026-07-17):** OltpDb → SQL Server CDC → **Debezium** raw → a .NET curation worker → **curated Avro** (Schema Registry, 10 order-flow contracts from a data-driven catalog) → the **StarRocks Kimball star** — SCD2 `dim_customer`/`dim_product` + 4 facts, loaded idempotently. Schema migrations are FluentMigrator (OltpDb, reversible up→down→up gate) + **DbUp** (StarRocks/ClickHouse, apply→re-apply gates on throwaway containers — which caught 4 defects in the authored DDL before any lab write). **The pipeline also observes itself (2026-07-19):** the curation and DWH-sink stages emit latency, end-to-end **CDC lag** and structured errors to Kafka, and **ClickHouse ingests that telemetry natively** through Kafka-engine tables + materialized views — no .NET consumer on the path — surfacing p50/p95/p99 per stage from an `AggregatingMergeTree`. Replay from zero: [`docs/handbook.md`](https://github.com/grezap/dataflow-studio/blob/main/docs/handbook.md). Remaining for `v0.1.0`: Marquez/OTel · tests to 80% · Aspire + K8s. |
| 2 | ⚪ | `tenantcore` | Clean Arch | Multi-tenant SaaS on Percona MySQL XtraDB + ProxySQL |
| 3 | ⚪ | `sentinelml` | Vertical Slice | Fraud + anomaly ML with automated drift-triggered retraining |
| 4 | ⚪ | `localmind` | Clean Arch | Local LLM gateway — Semantic Kernel + Ollama + OpenAI-compatible API |
| 5 | ⚪ | `pulsenlp` | Vertical Slice | NLP pipeline: ML.NET + DistilBERT + BERT NER all served via ONNX in .NET |
| 6 | ⚪ | `visioncore` | Clean Arch | Computer vision — PyTorch → ONNX → C# inference, no OpenCV |
| 7 | ⚪ | `recoengine` | Modular Monolith | Recommendation engine — ML.NET MatrixFactorization + Kafka Streams |
| 8 | ⚪ | `chronosight` | Vertical Slice | Time-series forecasting on ClickHouse + ksqlDB |
| 9 | ⚪ | `querylens` | Vertical Slice | SQL Server performance intelligence with AI rewrite suggestions |
| 10 | ⚪ | `fieldsync` | Clean Arch | .NET MAUI + gRPC bidirectional sync + on-device ONNX |
| 11 | ⚪ | `nexus-platform` | Microservices | 6-service reference: gRPC + Kafka + REST, 2 sagas, K8s manifests |
| 12 | ⚪ | `streamcore` | Vertical Slice | Kafka ecosystem showcase — Streams, ksqlDB, MirrorMaker 2 DR demo |
| 13 | ⚪ | `nexus-desk` | Monorepo | 4 native Windows apps: WinForms, WPF, WinUI 3, WinUI3+WPF hybrid |
| 14 | ⚪ | `lakehouse-core` | Medallion Lakehouse | Iceberg on MinIO (Bronze/Silver/Gold) + PySpark + dbt Core + Prefect + Trino federation |

## Highlights

- **.NET 10 / C# 13 throughout** — primary constructors, Generic Math, Native AOT, System.Threading.Channels, pattern-matching exhaustiveness.
- **Three deployment tiers** — VMware HA (Tier 1) · Docker Swarm + Nomad (Tier 2) · Kubernetes (Tier 3). The same project ships to all three.
- **Full observability from day one** — OpenTelemetry → Prometheus HA · Grafana HA (VRRP VIP) · **Loki on MinIO** · **Tempo on MinIO** · **OTel Collector pair** (OTLP gRPC :4317 + HTTP :4318) · **Vector fleet shipper** (every Linux fleet node ships journald + /var/log/* to Loki tagged `fleet=nexusplatform`), with W3C TraceContext propagation across every service boundary. The obs tier itself is HA across the LB tier per ADR-0025 — Grafana's single URL has atomic sub-second failover; Loki / Tempo memberlist rings tolerate node loss; both Proms scrape every fleet target with Grafana datasource dedup. C# and Python both push via OpenTelemetry SDKs + Serilog Loki sink / `python-logging-loki`. **Phase 0.I COMPLETE 2026-05-28 (ADR-0038); `grezap/nexus-infra-observability v0.1.0` tagged.** All 7 sub-phases SEALED live-ratified + cold-rebuild-proven; 14 obs VMs + 2 VRRP VIPs.
- **ML portability** — ML.NET, PyTorch, and HuggingFace models all served through ONNX in .NET with no Python runtime in production.

## Contact

- **Email:** `gzapas@gmail.com`
- **LinkedIn:** `https://www.linkedin.com/in/grigoris-zapantis-1a0638b/`
- **Upwork:** `https://www.upwork.com/freelancers/~01de3f0552684544ee`

## License

This repository (the index) is licensed under [MIT](./LICENSE). Individual project
repositories are licensed separately — see each repo's LICENSE file.
